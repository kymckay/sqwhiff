#pragma once

// Actual data types that exist in SQF at runtime, used for interpreting/analysis
enum SQFType
{
    any,
    array,
    boolean,
    code,
    config,
    control,
    diary_record,
    display,
    exception_type,
    for_type,
    group,
    hashmap,
    if_type,
    location,
    name_space,
    netobject,
    nothing,
    object,
    scalar,
    script,
    side,
    string,
    switch_type,
    task,
    team_member,
    text,
    while_type,
    with_type,
};